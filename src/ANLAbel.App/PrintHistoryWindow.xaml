<Window x:Class="ANLAbel.App.PrintHistoryWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ANLAbel - Print History" Width="1180" Height="720" MinWidth="900" MinHeight="560" WindowStartupLocation="CenterOwner" Loaded="Window_Loaded"
        AutomationProperties.AutomationId="CC.P5.History.Root" AutomationProperties.Name="Print history">
    <Window.Resources><SolidColorBrush x:Key="AppSurfaceBrush" Color="#FFFFFF"/><SolidColorBrush x:Key="AppBorderBrush" Color="#D9E2EC"/><SolidColorBrush x:Key="AppMutedBrush" Color="#64748B"/></Window.Resources>
    <Grid Margin="18"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <DockPanel Margin="0,0,0,14"><Button DockPanel.Dock="Right" Content="{Binding RefreshButtonText}" IsEnabled="{Binding CanRefresh}" Click="Refresh_Click" AutomationProperties.AutomationId="CC.P5.History.Refresh" AutomationProperties.Name="Refresh print history"/>
            <StackPanel><TextBlock Text="Print History" FontSize="23" FontWeight="SemiBold"/><TextBlock Text="Read-only provenance over durable lifecycle, supplemental operation trace and redacted CSV summaries." Margin="0,4,0,0" Foreground="{StaticResource AppMutedBrush}"/><TextBlock Text="{Binding StatusText}" Margin="0,7,0,0" FontWeight="SemiBold" AutomationProperties.AutomationId="CC.P5.History.SourceStatus" AutomationProperties.Name="History source status"/></StackPanel>
        </DockPanel>
        <Grid Grid.Row="1"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <Border Padding="10" Background="{StaticResource AppSurfaceBrush}" BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="1" CornerRadius="8" AutomationProperties.AutomationId="CC.P5.History.Filters" AutomationProperties.Name="History filters"><TextBox Width="320" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" ToolTip="Search job, template, queue or lifecycle" AutomationProperties.AutomationId="CC.P5.History.Search" AutomationProperties.Name="Search print history"/></Border>
            <DataGrid Grid.Row="1" Margin="0,12,0,0" ItemsSource="{Binding Rows}" SelectedItem="{Binding SelectedRow}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" AutomationProperties.AutomationId="CC.P5.History.ActivityTable" AutomationProperties.Name="Print activity"><DataGrid.Columns><DataGridTextColumn Header="Time" Binding="{Binding TimestampText}" Width="165"/><DataGridTextColumn Header="Job / record" Binding="{Binding RecordId}" Width="190"/><DataGridTextColumn Header="Type" Binding="{Binding RecordType}" Width="110"/><DataGridTextColumn Header="Template / queue" Binding="{Binding TemplateOrQueue}" Width="*"/><DataGridTextColumn Header="Lifecycle" Binding="{Binding Lifecycle}" Width="170"/><DataGridTextColumn Header="Evidence" Binding="{Binding Evidence}" Width="220"/><DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="155"/></DataGrid.Columns></DataGrid>
            <Border Grid.Row="2" Margin="0,12,0,0" Padding="12" Background="{StaticResource AppSurfaceBrush}" BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="1" CornerRadius="8" AutomationProperties.AutomationId="CC.P5.History.Detail" AutomationProperties.Name="Selected job evidence"><TextBlock Text="{Binding DetailText}" TextWrapping="Wrap" Foreground="{StaticResource AppMutedBrush}"/></Border>
        </Grid>
        <DockPanel Grid.Row="2" Margin="0,14,0,0"><Button DockPanel.Dock="Right" Content="Close" Click="Close_Click"/><StackPanel Orientation="Horizontal"><Button Content="Open Print Center" Click="Center_Click" AutomationProperties.AutomationId="CC.P5.History.OpenPrintCenter" AutomationProperties.Name="Open Print Center"/><TextBlock Text="Recovery and controlled reprint remain in Print Center." Margin="12,0,0,0" VerticalAlignment="Center" Foreground="{StaticResource AppMutedBrush}" AutomationProperties.AutomationId="CC.P5.History.ReprintEligibility" AutomationProperties.Name="Reprint eligibility"/></StackPanel></DockPanel>
    </Grid>
</Window>
