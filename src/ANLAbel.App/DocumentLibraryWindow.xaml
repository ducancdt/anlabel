<Window x:Class="ANLAbel.App.DocumentLibraryWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="ANLAbel - Document Library" Width="1000" Height="650" MinWidth="820" MinHeight="520" WindowStartupLocation="CenterOwner" Loaded="Window_Loaded" AutomationProperties.AutomationId="CC.P3.Library.Root" AutomationProperties.Name="Document library"><Window.Resources><SolidColorBrush x:Key="Surface" Color="#FFFFFF"/><SolidColorBrush x:Key="Border" Color="#D9E2EC"/><SolidColorBrush x:Key="Muted" Color="#64748B"/></Window.Resources><Grid Margin="18"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><DockPanel><Button DockPanel.Dock="Right" Content="Refresh" Click="Refresh_Click" AutomationProperties.AutomationId="CC.P3.Library.Refresh" AutomationProperties.Name="Refresh document library"/><StackPanel><TextBlock Text="Document Library" FontSize="23" FontWeight="SemiBold"/><TextBlock Text="{Binding RootPath}" AutomationProperties.AutomationId="CC.P3.Library.RootStatus" AutomationProperties.Name="Local library root status"/><TextBlock Text="{Binding RootStatus}" Foreground="{StaticResource Muted}" TextWrapping="Wrap"/></StackPanel></DockPanel><TextBox Grid.Row="1" Margin="0,14,0,12" Width="320" HorizontalAlignment="Left" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" ToolTip="Search local documents" AutomationProperties.AutomationId="CC.P3.Library.Search" AutomationProperties.Name="Search documents"/><DataGrid Grid.Row="2" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" AutomationProperties.AutomationId="CC.P3.Library.FileResults" AutomationProperties.Name="Documents in selected folder"><DataGrid.Columns><DataGridTextColumn Header="Relative path" Binding="{Binding RelativePath}" Width="*"/><DataGridTextColumn Header="Size" Binding="{Binding SizeBytes}" Width="100"/><DataGridTextColumn Header="Modified" Binding="{Binding LastWriteLocal, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="170"/><DataGridTextColumn Header="Validation" Binding="{Binding Validation}" Width="260"/></DataGrid.Columns></DataGrid><DockPanel Grid.Row="3" Margin="0,14,0,0"><Button DockPanel.Dock="Right" Content="Close" Click="Close_Click"/><StackPanel Orientation="Horizontal"><Button Content="Open selected" Click="Open_Click" AutomationProperties.AutomationId="CC.P3.Library.Preview" AutomationProperties.Name="Preview selected document"/><Button Content="Revision history" Margin="8,0,0,0" Click="Revision_Click" AutomationProperties.AutomationId="CC.P3.Library.Revisions" AutomationProperties.Name="Document revisions"/></StackPanel></DockPanel></Grid></Window>
